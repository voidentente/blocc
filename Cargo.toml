[profile.dev.package."*"]
opt-level = 3

[profile.release]
lto = true
codegen-units = 1

[workspace]

resolver = "2"

members = [
    "blocc_game",
    "blocc_server",
    "client/client_state",
    "client/launcher",
    "common/player_identity",
    "common/bevy_rapid_qoi",
]